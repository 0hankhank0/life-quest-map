import type { CustomSocialQuote } from "@/components/SocialPostCard";

export function CustomQuoteEditor({ value, onChange, isValid }: { value: CustomSocialQuote; onChange: (next: CustomSocialQuote) => void; isValid: boolean }) {
  return <div className="mb-4 grid gap-3"><label className="text-sm font-bold">語錄正文<textarea aria-label="語錄正文" value={value.text} maxLength={240} onChange={(event) => onChange({ ...value, text: event.target.value })} className="field-control mt-1 min-h-28" /></label><p className="text-xs text-zinc-400">{value.text.length}/240 字</p><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm font-bold">說話者／作者<input aria-label="說話者／作者" value={value.author} maxLength={80} onChange={(event) => onChange({ ...value, author: event.target.value })} className="field-control mt-1" /></label><label className="text-sm font-bold">作品／出處<input aria-label="作品／出處" value={value.work} maxLength={120} onChange={(event) => onChange({ ...value, work: event.target.value })} className="field-control mt-1" /></label></div><div className="flex gap-2"><button type="button" onClick={() => onChange({ text: "", author: "", work: "" })} className="rounded-lg border border-white/15 px-3 py-2 font-bold">清除</button></div>{!isValid ? <p role="status" className="text-sm text-amber-100">請輸入語錄正文後即可預覽與下載。</p> : null}</div>;
}
